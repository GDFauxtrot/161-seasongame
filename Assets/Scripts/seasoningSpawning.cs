using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class seasoningSpawning : MonoBehaviour
{
    //number of spawn locations
    public int numOfSpawnLocations;
    public GameObject spawnManager;
    //The transform locations for seasoning
    private List<GameObject> spawnLocations;
    // Start is called before the first frame update
    void Start()
    {
        for (int i = 0; i < numOfSpawnLocations; i++)
        {

        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
